{
  "name": "mime-app",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:maps": "node scripts/build-maps.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "ci": "npm run build:maps && npm test && npm run build",
    "export:android": "node scripts/export-maps-android.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "~6.0.2",
    "vite": "^8.3.0",
    "vitest": "^3.2.7"
  },
  "dependencies": {
    "chess.js": "^1.4.0",
    "chessground": "^9.2.1"
  }
}
